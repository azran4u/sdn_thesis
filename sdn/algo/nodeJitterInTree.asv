function [ jitter ] = nodeJitterInTree( v, ck, lk )
sckiTree=treeIndex(ck, lk);
    latency = G.Nodes.treeLatency(v,sckiTree);
end

function [ latency ] = nodeLatencyInTree( v, ck, lk )

    sckiTree=treeIndex(ck, lk);
    latency = G.Nodes.treeLatency(v,sckiTree);

end

